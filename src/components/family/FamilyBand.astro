---
interface Props {
  name: string;
  count: string;
}

const { name, count } = Astro.props;
---
<div class="fam-band">
  <span class="dot"></span>
  <h3>{name}</h3>
  <span class="cnt">{count}</span>
</div>
