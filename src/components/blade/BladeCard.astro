---
import BladeFeatures from '@/components/blade/BladeFeatures.astro';
import MaterialRow from '@/components/blade/MaterialRow.astro';
import SawBladeSvg from '@/components/blade/SawBladeSvg.astro';
import Icon from '@/components/icons/Icon.astro';
import MatBadge from '@/components/shared/MatBadge.astro';
import type { BladeRecord } from '@/types/catalog';

interface Props {
  blade: BladeRecord;
}

const { blade } = Astro.props;
---

<div class="card">
  <div class="top">
    <div class="blade-id">
      {blade.ref}
    </div>
    <div class="name">
      <b>{blade.name}</b>
      <span class="name-meta">
        Longueur totale {blade.length} &middot; Longueur utile {blade.usefulLength}
      </span>
    </div>
    <BladeFeatures features={blade.features} />
  </div>
  <div class="mid">
    <div class="blade-col">
      <div class="bmat">
        <span class="section-label">Lame</span>
        <MatBadge code={blade.material} />
      </div>
      <div class="saw-wrap">
        <SawBladeSvg sawPath={blade.sawPath} />
      </div>
      <div class="tpi">
        <Icon name="tpi" size={18} />
        <b>{blade.tpi}</b>
        <span style="color:var(--slate)">TPI</span>
      </div>
    </div>
    <div class="cuts">
      <div class="section-label">Matériaux pouvant être coupés</div>
      <MaterialRow materials={blade.materials} />
    </div>
  </div>
  <div class="specs">
    <div class="spec">
      <div class="field-label">Longueur totale</div>
      <div class="field-value">{blade.length}</div>
    </div>
    <div class="spec">
      <div class="field-label">Longueur utile</div>
      <div class="field-value">{blade.usefulLength}</div>
    </div>
    <div class="spec">
      <div class="field-label">Denture</div>
      <div class="field-value">{blade.toothType}</div>
    </div>
    <div class="spec">
      <div class="field-label">Equiv. Bosch</div>
      <div class="field-value mono">{blade.boschEquiv}</div>
    </div>
  </div>
  {blade.cutThickness && (
    <div class="thick">
      <span class="field-label">Épaisseur de coupe</span>{blade.cutThickness}
    </div>
  )}
  <div class="observations" set:html={blade.observationsHtml} />
</div>
