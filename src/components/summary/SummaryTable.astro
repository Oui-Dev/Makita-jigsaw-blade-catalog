---
import MatBadge from '@/components/shared/MatBadge.astro';
import SummaryCheck from '@/components/shared/SummaryCheck.astro';
import { bladeRefs, blades } from '@/data/blades';

const rows = bladeRefs.map((ref) => blades[ref].summary).filter((row) => row != null);
---

<div class="sec-head">
  <span class="num">02</span>
  <h2>Tableau recapitulatif</h2>
  <div class="desc">37 references compatibles, vue d'ensemble.</div>
</div>
<table class="sum">
  <thead>
    <tr>
      <th>Ref</th>
      <th class="c">Lame</th>
      <th class="c">TPI</th>
      <th class="c">Longueur</th>
      <th>Principaux materiaux</th>
      <th class="c">Rapide</th>
      <th class="c">Propre</th>
      <th class="c">Courbe</th>
      <th class="c">Lubrif.</th>
    </tr>
  </thead>
  <tbody>
    {rows.map((row) => (
      <tr>
        <td class="ref">{row.ref}</td>
        <td class="c"><MatBadge code={row.material} /></td>
        <td class="c">{row.tpi}</td>
        <td class="c">{row.length}</td>
        <td>{row.mainMaterials}</td>
        <td class="c"><SummaryCheck value={row.fast} /></td>
        <td class="c"><SummaryCheck value={row.clean} /></td>
        <td class="c"><SummaryCheck value={row.curved} /></td>
        <td class="c"><SummaryCheck value={row.lubrication} /></td>
      </tr>
    ))}
  </tbody>
</table>
